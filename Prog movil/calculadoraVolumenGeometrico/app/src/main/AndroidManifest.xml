<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalculadoraDeVolumenGeometrico"
        tools:targetApi="31">
        <activity
            android:name=".ConeCircularActivity"
            android:exported="false" />
        <activity
            android:name=".SphereActivity"
            android:exported="false"
            android:label="@string/title_activity_sphere"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.CalculadoraDeVolumenGeometrico" />
        <activity
            android:name=".CylinderActivity"
            android:exported="false"
            android:label="@string/title_activity_cylinder"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.CalculadoraDeVolumenGeometrico" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CalculadoraDeVolumenGeometrico">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>